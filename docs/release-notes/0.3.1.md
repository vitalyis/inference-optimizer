# Release Notes: v0.3.1

## Summary

ClawHub / OpenClaw registry metadata only: `SKILL.md` frontmatter now declares install commands (`cliHelp`), on-disk state paths (`config.stateDirs` and `config.example`), repository links, and `requires.bins` under `metadata.openclaw` so automated security review can align declared footprint with the skill’s behavior.

## Files updated

- `SKILL.md`
- `CHANGELOG.md`
